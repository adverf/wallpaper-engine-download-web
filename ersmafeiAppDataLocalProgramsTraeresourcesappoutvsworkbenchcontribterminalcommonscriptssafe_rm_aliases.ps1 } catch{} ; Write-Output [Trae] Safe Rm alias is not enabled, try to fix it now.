[1mdiff --git a/index.html b/index.html[m
[1mindex a57ca23..3527b02 100644[m
[1m--- a/index.html[m
[1m+++ b/index.html[m
[36m@@ -373,9 +373,11 @@[m [mif(API_PARAM) localStorage.setItem('wallhub_api_base', API_BASE);[m
 const S = {[m
   page:1, totalPages:1, totalItems:0,[m
   loading:false, view:'grid',[m
[32m+[m[32m  loadToken:0, modalId:'',[m
   items:[],[m
   f:{ search:'', sort:'trend', days:'30', type:'', rating:'', genres:[] }[m
 };[m
[32m+[m[32mconst DETAIL_CACHE = new Map();[m
 [m
 function apiUrl(path){[m
   if(!path) return API_BASE || '';[m
[36m@@ -563,6 +565,8 @@[m [mfunction buildParams(){[m
 ================================================================ */[m
 async function load(){[m
   if(S.loading) return;[m
[32m+[m[32m  const loadToken = Date.now();[m
[32m+[m[32m  S.loadToken = loadToken;[m
   S.loading=true;[m
   showLoading();[m
   try {[m
[36m@@ -603,6 +607,9 @@[m [masync function load(){[m
       document.getElementById('resCnt').textContent = `约 ${dispTotal} 个 · 共 ${S.totalPages} 页`;[m
       renderItems(S.items);[m
       renderPagination();[m
[32m+[m[32m      if(!inBackendMode()){[m
[32m+[m[32m        enrichItemsViaProxy(S.items, loadToken).catch(()=>{});[m
[32m+[m[32m      }[m
     }[m
   } catch(err){[m
     console.error(err);[m
[36m@@ -765,6 +772,7 @@[m [mfunction goPage(p){ if(p<1||p>S.totalPages||p===S.page) return; S.page=p; window[m
 function openModal(id){[m
   const item = S.items.find(w=>w.publishedfileid===id);[m
   if(!item) return;[m
[32m+[m[32m  S.modalId = id;[m
 [m
   const thumb = item.preview_url||'';[m
   document.getElementById('mTitle').textContent = item.title||'未命名壁纸';[m
[36m@@ -794,7 +802,25 @@[m [mfunction openModal(id){[m
 [m
   document.getElementById('mOv').classList.add('open');[m
   document.body.style.overflow='hidden';[m
[31m-  if(!inBackendMode()){ renderCmts([]); return; }[m
[32m+[m[32m  if(!inBackendMode()){[m
[32m+[m[32m    fetchProxyDetail(id).then(d=>{[m
[32m+[m[32m      if(S.modalId !== id) return;[m
[32m+[m[32m      if(d.preview_url) document.getElementById('mImg').src=d.preview_url;[m
[32m+[m[32m      if(d.description) document.getElementById('mDesc').textContent=d.description;[m
[32m+[m[32m      if(d.author) document.getElementById('mSub').innerHTML=`<span>🆔 ${id}</span><span>作者: ${esc(d.author)}</span>`;[m
[32m+[m[32m      if(d.tags && d.tags.length) document.getElementById('mTags').innerHTML=d.tags.map(t=>`<span class="tag-chip">${esc(t)}</span>`).join('');[m
[32m+[m[32m      renderStats({[m
[32m+[m[32m        subs:  fmtN(d.subscriptions || item.subscriptions || 0),[m
[32m+[m[32m        favs:  fmtN(d.favorited || item.favorited || 0),[m
[32m+[m[32m        views: fmtN(d.views || item.views || 0),[m
[32m+[m[32m        size:  d.file_size || '未知',[m
[32m+[m[32m        upd:   d.time_updated || '未知',[m
[32m+[m[32m        id,[m
[32m+[m[32m      });[m
[32m+[m[32m      renderCmts(d.comments||[]);[m
[32m+[m[32m    }).catch(()=>renderCmts([]));[m
[32m+[m[32m    return;[m
[32m+[m[32m  }[m
 [m
   // Fetch details[m
   fetch(apiUrl(`/api/steam/details?id=${id}`))[m
[36m@@ -846,6 +872,7 @@[m [mfunction renderCmts(list){[m
 function closeModal(){[m
   document.getElementById('mOv').classList.remove('open');[m
   document.body.style.overflow='';[m
[32m+[m[32m  S.modalId = '';[m
 }[m
 function mOvClick(e){ if(e.target===document.getElementById('mOv')) closeModal(); }[m
 [m
[36m@@ -854,11 +881,7 @@[m [mfunction mOvClick(e){ if(e.target===document.getElementById('mOv')) closeModal()[m
 ================================================================ */[m
 function dlWall(fid, title){[m
   if(!inBackendMode()){[m
[31m-    const page = `https://steamcommunity.com/sharedfiles/filedetails/?id=${fid}`;[m
[31m-    const steamUri = `steam://openurl/${page}`;[m
[31m-    window.open(steamUri, '_blank');[m
[31m-    setTimeout(()=>window.open(page, '_blank'), 120);[m
[31m-    toast(`已跳转订阅页面：${title||fid}`, 'ok');[m
[32m+[m[32m    toast('未配置下载后端，请在地址后追加 ?api=https://你的后端域名', 'warn');[m
     return;[m
   }[m
   const btn=document.getElementById(`sub-${fid}`);[m
[36m@@ -940,6 +963,79 @@[m [mfunction getType(item){[m
   if(ts.includes('web'))         return 'Web';[m
   return 'Scene';[m
 }[m
[32m+[m[32mfunction parseSteamNumber(v){[m
[32m+[m[32m  return parseInt(String(v||'').replace(/[^\d]/g,'')) || 0;[m
[32m+[m[32m}[m
[32m+[m[32mfunction parseSteamDetailMarkdown(md, id){[m
[32m+[m[32m  const type = (md.match(/Type:\[([^\]]+)\]/i)||[])[1] || '';[m
[32m+[m[32m  const rating = (md.match(/Age Rating:\[([^\]]+)\]/i)||[])[1] || '';[m
[32m+[m[32m  const genre = (md.match(/Genre:\[([^\]]+)\]/i)||[])[1] || '';[m
[32m+[m[32m  const views = parseSteamNumber((md.match(/([\d,]+)\s+Unique Visitors/i)||[])[1]);[m
[32m+[m[32m  const subs = parseSteamNumber((md.match(/([\d,]+)\s+Current Subscribers/i)||[])[1]);[m
[32m+[m[32m  const favs = parseSteamNumber((md.match(/([\d,]+)\s+Current Favorites/i)||[])[1]);[m
[32m+[m[32m  const size = ((md.match(/File Size[\s\S]{0,220}?(\d[\d.,]*\s*(?:TB|GB|MB|KB|B))/i)||[])[1] || '').trim();[m
[32m+[m[32m  const updated = ((md.match(/File Size[\s\S]{0,260}?\n\s*\d[\d.,]*\s*(?:TB|GB|MB|KB|B)\s*\n+\s*([^\n]+?@[^\\n]+|[A-Za-z]{3}\s+\d{1,2}[^\n]*)/i)||[])[1] || '').trim();[m
[32m+[m[32m  const author = ((md.match(/Created by[\s\S]{0,300}\n\s*([^\n]+)\n\s*(?:Offline|Online)\b/i)||[])[1] || '').trim();[m
[32m+[m[32m  const desc = ((md.match(/Description\s*\n+([\s\S]*?)(?:\n+\d+\s+Comments|\n+\[\<\]|\n+Edit links)/i)||[])[1] || '').trim();[m
[32m+[m[32m  const comments = [];[m
[32m+[m[32m  const cRe = /\[([^\]]+)\]\(https?:\/\/steamcommunity\.com\/(?:profiles|id)\/[^)]+\)\s+([^\n]+)\n\n([\s\S]*?)(?=\n\s*\[![^\n]+\]\(https?:\/\/avatars\.|\n+\[\<\]|\n+$)/g;[m
[32m+[m[32m  let m;[m
[32m+[m[32m  while((m = cRe.exec(md))){[m
[32m+[m[32m    const text = m[3].trim();[m
[32m+[m[32m    if(!text) continue;[m
[32m+[m[32m    comments.push({ author:m[1].trim(), date:m[2].trim(), text });[m
[32m+[m[32m    if(comments.length >= 30) break;[m
[32m+[m[32m  }[m
[32m+[m[32m  const tags = [type, rating, genre].filter(Boolean);[m
[32m+[m[32m  return {[m
[32m+[m[32m    publishedfileid:id,[m
[32m+[m[32m    tags,[m
[32m+[m[32m    type,[m
[32m+[m[32m    subscriptions:subs,[m
[32m+[m[32m    favorited:favs,[m
[32m+[m[32m    views,[m
[32m+[m[32m    file_size:size,[m
[32m+[m[32m    time_updated:updated,[m
[32m+[m[32m    author,[m
[32m+[m[32m    description:desc,[m
[32m+[m[32m    comments,[m
[32m+[m[32m  };[m
[32m+[m[32m}[m
[32m+[m[32masync function fetchProxyDetail(id){[m
[32m+[m[32m  if(DETAIL_CACHE.has(id)) return DETAIL_CACHE.get(id);[m
[32m+[m[32m  const page = `https://steamcommunity.com/sharedfiles/filedetails/?id=${id}`;[m
[32m+[m[32m  const res = await fetch(proxyUrl(page), { cache:'no-store' });[m
[32m+[m[32m  if(!res.ok) throw new Error(`detail ${res.status}`);[m
[32m+[m[32m  const md = await res.text();[m
[32m+[m[32m  const d = parseSteamDetailMarkdown(md, id);[m
[32m+[m[32m  DETAIL_CACHE.set(id, d);[m
[32m+[m[32m  return d;[m
[32m+[m[32m}[m
[32m+[m[32masync function enrichItemsViaProxy(items, token){[m
[32m+[m[32m  const queue = (items||[]).map(it=>String(it.publishedfileid||'')).filter(Boolean);[m
[32m+[m[32m  const conc = 6;[m
[32m+[m[32m  let idx = 0;[m
[32m+[m[32m  async function worker(){[m
[32m+[m[32m    while(idx < queue.length){[m
[32m+[m[32m      const id = queue[idx++];[m
[32m+[m[32m      try{[m
[32m+[m[32m        const d = await fetchProxyDetail(id);[m
[32m+[m[32m        const it = S.items.find(x=>String(x.publishedfileid)===id);[m
[32m+[m[32m        if(!it) continue;[m
[32m+[m[32m        if(d.subscriptions) it.subscriptions = d.subscriptions;[m
[32m+[m[32m        if(d.favorited) it.favorited = d.favorited;[m
[32m+[m[32m        if(d.views) it.views = d.views;[m
[32m+[m[32m        if(d.file_size) it.file_size = d.file_size;[m
[32m+[m[32m        if(d.time_updated) it.time_updated = d.time_updated;[m
[32m+[m[32m        if(d.tags && d.tags.length) it.tags = d.tags.map(t=>({tag:t}));[m
[32m+[m[32m      }catch{}[m
[32m+[m[32m    }[m
[32m+[m[32m  }[m
[32m+[m[32m  await Promise.all(Array.from({length:conc}, ()=>worker()));[m
[32m+[m[32m  if(token === S.loadToken){[m
[32m+[m[32m    renderItems(S.items);[m
[32m+[m[32m  }[m
[32m+[m[32m}[m
 function fmtN(n){ n=parseInt(n)||0; if(n>=1e6) return (n/1e6).toFixed(1)+'M'; if(n>=1e3) return (n/1e3).toFixed(1)+'K'; return n.toString(); }[m
 function fmtBytes(b){ b=parseInt(b)||0; if(!b) return '未知'; if(b>=1073741824) return (b/1073741824).toFixed(1)+' GB'; if(b>=1048576) return (b/1048576).toFixed(1)+' MB'; if(b>=1024) return (b/1024).toFixed(1)+' KB'; return b+' B'; }[m
 function fmtTime(ts){ ts=parseInt(ts); if(!ts) return '未知'; return new Date(ts*1000).toLocaleDateString('zh-CN',{year:'numeric',month:'2-digit',day:'2-digit'}); }[m
